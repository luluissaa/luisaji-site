{
  "name": "luisaji-site",
  "private": true,
  "version": "0.1.0",
  "description": "luisaji.com — static site built from JSON content files. See README.md.",
  "scripts": {
    "validate": "node scripts/validate.js",
    "build": "node scripts/build.js",
    "preview": "npm run validate && npm run build && npx --yes serve dist"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
